# 改进计划 1 - PrecondAI 优化方案

## 版本信息
- **创建日期**: 2026-04-27
- **目标版本**: 1.0.1
- **优先级**: 高

---

## 一、需求分析与问题清单

### 1.1 当前问题汇总

| 编号 | 问题描述 | 影响范围 | 优先级 |
|------|---------|---------|--------|
| P1 | 位置服务未实现，WeatherService依赖CoreLocation但无CLLocationManager | 天气功能 | 🔴 阻塞 |
| P2 | Info.plist缺少NSLocationWhenInUseUsageDescription权限描述 | 天气功能 | 🔴 阻塞 |
| P3 | WeatherAPIClient依赖OpenWeatherMap API Key（需注册） | 天气功能 | 🟡 高 |
| P4 | DashboardViewModel硬编码TeslaAPIClient初始化 | 架构/可维护性 | � 阻塞 |
| P5 | Info.plist缺少BGTaskSchedulerPermittedIdentifiers配置 | 后台任务 | 🟡 高 |
| P6 | 缺少统一的依赖注入/服务管理机制 | 全局架构 | 🟢 中 |
| P7 | Tesla OAuth授权流程缺失，用户无法连接自己的车辆 | 核心功能 | 🔴 阻塞 |
| P8 | VehicleAuthView仅保存本地信息，无真实OAuth授权 | 核心功能 | 🔴 阻塞 |
| P9 | Token存储不安全，未使用Keychain | 安全 | 🟡 高 |

### 1.2 优化目标

1. **天气数据方案**: 评估使用免费天气API vs Apple WeatherKit
2. **位置服务**: 实现完整的CLLocationManager权限请求流程
3. **Tesla OAuth**: 实现完整的用户OAuth授权流程，连接用户自己的车辆
4. **Token安全**: 使用Keychain安全存储用户访问令牌
5. **架构优化**: 消除硬编码，引入服务容器
6. **配置完善**: 补充所有缺失的Info.plist和Capabilities配置

---

## 二、天气数据方案选型

### 2.1 方案对比

| 方案 | 优点 | 缺点 | 成本 | 推荐度 |
|------|------|------|------|--------|
| **A. OpenWeatherMap (现有)** | 已实现、免费额度1000次/天 | 需API Key、非苹果原生 | 免费 | ⭐⭐⭐ |
| **B. Apple WeatherKit** | 苹果原生、无需Key、隐私好 | 需iOS 16+、需配置CloudKit | 含在开发者计划 | ⭐⭐⭐⭐⭐ |
| **C. Open-Meteo API** | 完全免费、无需Key、开源 | 非苹果原生 | 免费 | ⭐⭐⭐⭐ |

### 2.2 推荐方案: Apple WeatherKit

**选择理由**:
- 符合"Apple Native First"原则
- 无需第三方API Key注册
- 与iOS系统深度集成
- 包含在$99/年开发者计划中
- 隐私保护更好

**技术要求**:
- 最低iOS版本: 16.0 (当前项目17.0 ✅)
- 需要配置: WeatherKit Capability
- 需要配置: iCloud Container (可选)

---

## 三、Tesla OAuth授权流程设计

### 3.0 架构设计原则

**核心逻辑**: 
- **开发者（你）**: 在Tesla Developer Portal注册应用，获得`client_id`和`client_secret`
- **用户（客户）**: 通过OAuth 2.0授权流程连接**自己的**Tesla车辆
- **Token存储**: 用户的`access_token`和`refresh_token`安全存储在设备Keychain中

**OAuth 2.0授权码流程**:
```
1. 应用生成authorize URL → 跳转到Tesla登录页
2. 用户登录Tesla账号 → 授权应用访问车辆
3. Tesla回调URL → 携带authorization_code
4. 应用用code换取access_token → 存储到Keychain
5. 使用access_token → 访问用户车辆API
6. Token过期 → 自动用refresh_token刷新
```

### 3.0.1 模块6: Tesla OAuth管理器 (TeslaOAuthManager)

**需求描述**: 
实现完整的Tesla OAuth 2.0授权流程，让用户安全连接自己的车辆。

**功能模块原则**:
- 单一职责: 仅负责OAuth授权和Token管理
- 高内聚低耦合: 独立模块，通过回调暴露授权结果
- 命名清晰: `TeslaOAuthManager`
- 文件结构: `Core/Auth/TeslaOAuthManager.swift`

**实现代码**:

```swift
import Foundation
import AuthenticationServices

@Observable
final class TeslaOAuthManager: NSObject, Observable {
    static let shared = TeslaOAuthManager()
    
    private let clientId: String
    private let redirectURI: String
    private let scope = "openid offline_access vehicle_device_data vehicle_cmds vehicle_charging_cmds"
    private var currentSession: ASWebAuthenticationSession?
    private var continuation: CheckedContinuation<TeslaAuthResponse, Error>?
    
    var isAuthenticating = false
    var isAuthenticated = false
    var currentVehicle: Vehicle?
    
    private init() {
        self.clientId = Bundle.main.infoDictionary?["TESLA_CLIENT_ID"] as? String ?? ""
        self.redirectURI = "precondai://auth/tesla/callback"
        super.init()
    }
    
    func startAuthFlow() async throws -> TeslaAuthResponse {
        guard !clientId.isEmpty else {
            throw TeslaAuthError.missingClientId
        }
        
        isAuthenticating = true
        defer { isAuthenticating = false }
        
        let authURL = buildAuthorizeURL()
        guard let url = URL(string: authURL) else {
            throw TeslaAuthError.invalidURL
        }
        
        return try await withCheckedThrowingContinuation { continuation in
            self.continuation = continuation
            currentSession = ASWebAuthenticationSession(
                url: url,
                callbackURLScheme: "precondai"
            ) { callbackURL, error in
                self.handleCallback(callbackURL, error: error)
            }
            currentSession?.presentationContextProvider = self
            currentSession?.prefersEphemeralWebBrowserSession = true
            currentSession?.start()
        }
    }
    
    private func buildAuthorizeURL() -> String {
        let state = UUID().uuidString
        let codeChallenge = generateCodeChallenge()
        var components = URLComponents(string: "https://auth.tesla.com/oauth2/v3/authorize")!
        components.queryItems = [
            URLQueryItem(name: "client_id", value: clientId),
            URLQueryItem(name: "redirect_uri", value: redirectURI),
            URLQueryItem(name: "response_type", value: "code"),
            URLQueryItem(name: "scope", value: scope),
            URLQueryItem(name: "state", value: state),
            URLQueryItem(name: "code_challenge", value: codeChallenge),
            URLQueryItem(name: "code_challenge_method", value: "S256"),
            URLQueryItem(name: "prompt", value: "login")
        ]
        return components.url!.absoluteString
    }
    
    private func handleCallback(_ callbackURL: URL?, error: Error?) {
        if let error = error {
            continuation?.resume(throwing: TeslaAuthError.cancelled(error))
            continuation = nil
            return
        }
        
        guard let url = callbackURL,
              let components = URLComponents(url: url, resolvingAgainstBaseURL: true),
              let code = components.queryItems?.first(where: { $0.name == "code" })?.value else {
            continuation?.resume(throwing: TeslaAuthError.invalidCallback)
            continuation = nil
            return
        }
        
        Task {
            do {
                let authResponse = try await exchangeCodeForToken(code: code)
                try KeychainTokenStorage.saveAuthResponse(authResponse)
                isAuthenticated = true
                continuation?.resume(returning: authResponse)
                continuation = nil
            } catch {
                continuation?.resume(throwing: error)
                continuation = nil
            }
        }
    }
    
    private func exchangeCodeForToken(code: String) async throws -> TeslaAuthResponse {
        let tokenURL = "https://auth.tesla.com/oauth2/v3/token"
        var request = URLRequest(url: URL(string: tokenURL)!)
        request.httpMethod = "POST"
        request.setValue("application/x-www-form-urlencoded", forHTTPHeaderField: "Content-Type")
        
        let body = [
            "grant_type": "authorization_code",
            "client_id": clientId,
            "code": code,
            "redirect_uri": redirectURI,
            "code_verifier": getCodeVerifier()
        ]
        request.httpBody = body.map { "\($0.key)=\($0.value)" }.joined(separator: "&").data(using: .utf8)
        
        let (data, response) = try await URLSession.shared.data(for: request)
        guard let httpResponse = response as? HTTPURLResponse,
              (200...299).contains(httpResponse.statusCode) else {
            throw TeslaAuthError.tokenExchangeFailed
        }
        
        return try JSONDecoder().decode(TeslaAuthResponse.self, from: data)
    }
    
    func refreshToken() async throws -> TeslaAuthResponse {
        guard let refreshToken = try KeychainTokenStorage.getRefreshToken() else {
            throw TeslaAuthError.noRefreshToken
        }
        
        let tokenURL = "https://auth.tesla.com/oauth2/v3/token"
        var request = URLRequest(url: URL(string: tokenURL)!)
        request.httpMethod = "POST"
        request.setValue("application/x-www-form-urlencoded", forHTTPHeaderField: "Content-Type")
        
        let body = [
            "grant_type": "refresh_token",
            "client_id": clientId,
            "refresh_token": refreshToken
        ]
        request.httpBody = body.map { "\($0.key)=\($0.value)" }.joined(separator: "&").data(using: .utf8)
        
        let (data, response) = try await URLSession.shared.data(for: request)
        guard let httpResponse = response as? HTTPURLResponse,
              (200...299).contains(httpResponse.statusCode) else {
            throw TeslaAuthError.tokenRefreshFailed
        }
        
        let authResponse = try JSONDecoder().decode(TeslaAuthResponse.self, from: data)
        try KeychainTokenStorage.saveAuthResponse(authResponse)
        return authResponse
    }
    
    func signOut() {
        try? KeychainTokenStorage.clearAll()
        isAuthenticated = false
        currentVehicle = nil
    }
}

// MARK: - ASWebAuthenticationPresentationContextProviding
extension TeslaOAuthManager: ASWebAuthenticationPresentationContextProviding {
    func presentationAnchor(for session: ASWebAuthenticationSession) -> ASPresentationAnchor {
        return UIApplication.shared.connectedScenes
            .compactMap { $0 as? UIWindowScene }
            .first?.windows
            .first ?? UIWindow()
    }
}

// MARK: - PKCE Code Challenge
extension TeslaOAuthManager {
    private func generateCodeVerifier() -> String {
        var buffer = [UInt8](repeating: 0, count: 32)
        _ = SecRandomCopyBytes(kSecRandomDefault, buffer.count, &buffer)
        return Data(buffer).base64URLEncodedString()
    }
    
    private func generateCodeChallenge() -> String {
        let verifier = generateCodeVerifier()
        let hash = SHA256.hash(data: verifier.data(using: .utf8)!)
        return Data(hash).base64URLEncodedString()
    }
    
    private func getCodeVerifier() -> String {
        return generateCodeVerifier()
    }
}

// MARK: - Models
struct TeslaAuthResponse: Codable {
    let accessToken: String
    let refreshToken: String
    let expiresIn: Int
    let tokenType: String
    
    enum CodingKeys: String, CodingKey {
        case accessToken = "access_token"
        case refreshToken = "refresh_token"
        case expiresIn = "expires_in"
        case tokenType = "token_type"
    }
}

enum TeslaAuthError: LocalizedError {
    case missingClientId
    case invalidURL
    case cancelled(Error)
    case invalidCallback
    case tokenExchangeFailed
    case tokenRefreshFailed
    case noRefreshToken
    
    var errorDescription: String? {
        switch self {
        case .missingClientId: return "Tesla client ID not configured"
        case .invalidURL: return "Invalid authorization URL"
        case .cancelled: return "Authentication cancelled"
        case .invalidCallback: return "Invalid callback from Tesla"
        case .tokenExchangeFailed: return "Failed to exchange code for token"
        case .tokenRefreshFailed: return "Failed to refresh token"
        case .noRefreshToken: return "No refresh token available"
        }
    }
}
```

### 3.0.2 模块7: Keychain Token存储 (KeychainTokenStorage)

**需求描述**: 
安全存储用户的Tesla访问令牌，使用iOS Keychain而非UserDefaults。

**功能模块原则**:
- 单一职责: 仅负责Keychain读写操作
- 静态方法: 无需实例化，直接调用
- 文件结构: `Core/Auth/KeychainTokenStorage.swift`

**实现代码**:

```swift
import Foundation
import Security

enum KeychainTokenStorage {
    private static let service = "com.zzoutuo.PrecondAI.tesla"
    private static let accessGroup = "JP4TN5PTS3.com.zzoutuo.PrecondAI"
    
    static func saveAuthResponse(_ response: TeslaAuthResponse) throws {
        try saveToken(response.accessToken, key: "access_token")
        try saveToken(response.refreshToken, key: "refresh_token")
    }
    
    static func getAccessToken() throws -> String? {
        return try getToken(key: "access_token")
    }
    
    static func getRefreshToken() throws -> String? {
        return try getToken(key: "refresh_token")
    }
    
    static func clearAll() throws {
        try deleteToken(key: "access_token")
        try deleteToken(key: "refresh_token")
    }
    
    private static func saveToken(_ token: String, key: String) throws {
        guard let data = token.data(using: .utf8) else {
            throw KeychainError.encodingFailed
        }
        
        let query: [String: Any] = [
            kSecClass as String: kSecClassGenericPassword,
            kSecAttrService as String: service,
            kSecAttrAccount as String: key,
            kSecValueData as String: data,
            kSecAttrAccessible as String: kSecAttrAccessibleAfterFirstUnlock
        ]
        
        SecItemDelete(query as CFDictionary)
        
        let status = SecItemAdd(query as CFDictionary, nil)
        guard status == errSecSuccess else {
            throw KeychainError.saveFailed(status)
        }
    }
    
    private static func getToken(key: String) throws -> String? {
        let query: [String: Any] = [
            kSecClass as String: kSecClassGenericPassword,
            kSecAttrService as String: service,
            kSecAttrAccount as String: key,
            kSecReturnData as String: true,
            kSecMatchLimit as String: kSecMatchLimitOne
        ]
        
        var result: AnyObject?
        let status = SecItemCopyMatching(query as CFDictionary, &result)
        
        if status == errSecItemNotFound {
            return nil
        }
        
        guard status == errSecSuccess,
              let data = result as? Data,
              let token = String(data: data, encoding: .utf8) else {
            throw KeychainError.readFailed(status)
        }
        
        return token
    }
    
    private static func deleteToken(key: String) throws {
        let query: [String: Any] = [
            kSecClass as String: kSecClassGenericPassword,
            kSecAttrService as String: service,
            kSecAttrAccount as String: key
        ]
        
        let status = SecItemDelete(query as CFDictionary)
        guard status == errSecSuccess || status == errSecItemNotFound else {
            throw KeychainError.deleteFailed(status)
        }
    }
}

enum KeychainError: LocalizedError {
    case encodingFailed
    case saveFailed(OSStatus)
    case readFailed(OSStatus)
    case deleteFailed(OSStatus)
    
    var errorDescription: String? {
        switch self {
        case .encodingFailed: return "Failed to encode token data"
        case .saveFailed(let status): return "Failed to save token: \(status)"
        case .readFailed(let status): return "Failed to read token: \(status)"
        case .deleteFailed(let status): return "Failed to delete token: \(status)"
        }
    }
}

extension Data {
    func base64URLEncodedString() -> String {
        return base64EncodedString()
            .replacingOccurrences(of: "+", with: "-")
            .replacingOccurrences(of: "/", with: "_")
            .replacingOccurrences(of: "=", with: "")
    }
}
```

### 3.0.3 模块8: 更新VehicleAuthView

**需求描述**: 
重构VehicleAuthView，添加Tesla OAuth授权按钮和状态显示。

**功能模块原则**:
- 复用现有Vehicle模型和SwiftData
- 保持现有UI风格一致
- 添加OAuth授权流程

**实现代码**:

```swift
import SwiftUI
import SwiftData

struct VehicleAuthView: View {
    @Environment(\.modelContext) private var modelContext
    @Environment(\.dismiss) private var dismiss
    @State private var selectedBrand: VehicleBrand = .tesla
    @State private var model = ""
    @State private var year = 2025
    @State private var displayName = ""
    @State private var isAuthenticating = false
    @State private var authError: String?
    @State private var showTeslaAuth = false
    
    var body: some View {
        Form {
            Section("Vehicle Brand") {
                Picker("Brand", selection: $selectedBrand) {
                    ForEach(VehicleBrand.allCases, id: \.self) { brand in
                        Text(brand.rawValue).tag(brand)
                    }
                }
            }
            
            Section("Vehicle Info") {
                TextField("Model (e.g. Model 3)", text: $model)
                TextField("Display Name (e.g. My Tesla)", text: $displayName)
                Picker("Year", selection: $year) {
                    ForEach((2020...2026).reversed(), id: \.self) { y in
                        Text(String(y)).tag(y)
                    }
                }
            }
            
            if selectedBrand == .tesla {
                Section("Tesla Connection") {
                    Button(action: { showTeslaAuth = true }) {
                        HStack {
                            Image(systemName: "bolt.car.fill")
                                .foregroundStyle(Color.appRed)
                            Text("Connect via Tesla Account")
                                .fontWeight(.medium)
                        }
                    }
                    .sheet(isPresented: $showTeslaAuth) {
                        TeslaAuthWebView()
                    }
                }
            }
            
            Section {
                Button(action: authenticate) {
                    HStack {
                        Spacer()
                        if isAuthenticating {
                            ProgressView()
                        } else {
                            Text("Add Vehicle")
                        }
                        Spacer()
                    }
                }
                .disabled(model.isEmpty || displayName.isEmpty || isAuthenticating)
            }
            
            if let error = authError {
                Section {
                    Text(error)
                        .foregroundStyle(.red)
                        .font(.caption)
                }
            }
        }
        .navigationTitle("Add Vehicle")
        .navigationBarTitleDisplayMode(.inline)
    }
    
    private func authenticate() {
        isAuthenticating = true
        authError = nil
        
        let vehicle = Vehicle(
            brand: selectedBrand.rawValue,
            model: model,
            year: year,
            displayName: displayName
        )
        
        modelContext.insert(vehicle)
        try? modelContext.save()
        
        isAuthenticating = false
        dismiss()
    }
}

struct TeslaAuthWebView: View {
    @Environment(\.dismiss) private var dismiss
    @State private var oauthManager = TeslaOAuthManager.shared
    @State private var isLoading = false
    @State private var errorMessage: String?
    
    var body: some View {
        NavigationStack {
            VStack(spacing: 24) {
                Image(systemName: "bolt.car.fill")
                    .font(.system(size: 64))
                    .foregroundStyle(Color.appRed)
                
                Text("Connect Your Tesla")
                    .font(.title2)
                    .fontWeight(.bold)
                
                Text("You'll be redirected to Tesla's secure login to authorize PrecondAI to access your vehicle.")
                    .font(.subheadline)
                    .foregroundStyle(.secondary)
                    .multilineTextAlignment(.center)
                    .padding(.horizontal, 32)
                
                Button(action: startAuth) {
                    HStack {
                        if isLoading {
                            ProgressView()
                        } else {
                            Text("Sign in with Tesla")
                                .fontWeight(.semibold)
                        }
                    }
                    .frame(maxWidth: .infinity)
                    .padding(.vertical, 16)
                    .foregroundStyle(.white)
                    .background(Color.appRed)
                    .clipShape(RoundedRectangle(cornerRadius: 12))
                }
                .disabled(isLoading)
                
                if let error = errorMessage {
                    Text(error)
                        .font(.caption)
                        .foregroundStyle(.red)
                        .padding(.top, 8)
                }
            }
            .padding(32)
            .navigationTitle("Tesla Authorization")
            .navigationBarTitleDisplayMode(.inline)
            .toolbar {
                ToolbarItem(placement: .cancellationAction) {
                    Button("Cancel") { dismiss() }
                }
            }
        }
    }
    
    private func startAuth() {
        isLoading = true
        errorMessage = nil
        
        Task {
            do {
                _ = try await oauthManager.startAuthFlow()
                await MainActor.run {
                    isLoading = false
                    dismiss()
                }
            } catch {
                await MainActor.run {
                    isLoading = false
                    errorMessage = error.localizedDescription
                }
            }
        }
    }
}
```

### 3.0.4 模块9: 更新TeslaAPIClient

**需求描述**: 
修改TeslaAPIClient，从Keychain获取用户Token而非硬编码。

**实现代码**:

```swift
import Foundation

actor TeslaAPIClient {
    private let baseURL = "https://fleet-api.prd.na.vn.cloud.tesla.com"
    
    private var accessToken: String? {
        get throws {
            return try KeychainTokenStorage.getAccessToken()
        }
    }
    
    func startPreconditioning(vin: String) async throws {
        let token = try await getValidToken()
        let endpoint = "/api/1/vehicles/\(vin)/command/auto_conditioning_start"
        try await sendCommand(endpoint: endpoint, body: [:], token: token)
    }
    
    func stopPreconditioning(vin: String) async throws {
        let token = try await getValidToken()
        let endpoint = "/api/1/vehicles/\(vin)/command/auto_conditioning_stop"
        try await sendCommand(endpoint: endpoint, body: [:], token: token)
    }
    
    func setTemperature(vin: String, driverTemp: Double, passengerTemp: Double? = nil) async throws {
        let token = try await getValidToken()
        let endpoint = "/api/1/vehicles/\(vin)/command/set_temps"
        var body: [String: Any] = ["driver_temp": driverTemp]
        if let pt = passengerTemp {
            body["passenger_temp"] = pt
        }
        try await sendCommand(endpoint: endpoint, body: body, token: token)
    }
    
    func getVehicleData(vin: String) async throws -> TeslaVehicleData {
        let token = try await getValidToken()
        let endpoint = "/api/1/vehicles/\(vin)/vehicle_data"
        return try await sendRequest(endpoint: endpoint, token: token)
    }
    
    func wakeUp(vin: String) async throws {
        let token = try await getValidToken()
        let endpoint = "/api/1/vehicles/\(vin)/wake_up"
        try await sendCommand(endpoint: endpoint, body: [:], token: token)
    }
    
    private func getValidToken() async throws -> String {
        guard let token = try accessToken else {
            throw APIError.unauthorized
        }
        return token
    }
    
    private func sendCommand(endpoint: String, body: [String: Any], token: String) async throws {
        let url = URL(string: baseURL + endpoint)!
        var request = URLRequest(url: url)
        request.httpMethod = "POST"
        request.setValue("Bearer \(token)", forHTTPHeaderField: "Authorization")
        request.setValue("application/json", forHTTPHeaderField: "Content-Type")
        if !body.isEmpty {
            request.httpBody = try JSONSerialization.data(withJSONObject: body)
        }
        let (_, response) = try await URLSession.shared.data(for: request)
        guard let httpResponse = response as? HTTPURLResponse else { throw APIError.invalidResponse }
        if httpResponse.statusCode == 401 { throw APIError.unauthorized }
        if httpResponse.statusCode == 429 { throw APIError.rateLimited }
        if !(200...299).contains(httpResponse.statusCode) { throw APIError.unknown("Command failed: \(httpResponse.statusCode)") }
    }
    
    private func sendRequest<T: Decodable>(endpoint: String, token: String) async throws -> T {
        let url = URL(string: baseURL + endpoint)!
        var request = URLRequest(url: url)
        request.httpMethod = "GET"
        request.setValue("Bearer \(token)", forHTTPHeaderField: "Authorization")
        let (data, response) = try await URLSession.shared.data(for: request)
        guard let httpResponse = response as? HTTPURLResponse else { throw APIError.invalidResponse }
        if httpResponse.statusCode == 401 { throw APIError.unauthorized }
        if !(200...299).contains(httpResponse.statusCode) { throw APIError.unknown("Request failed: \(httpResponse.statusCode)") }
        do {
            return try JSONDecoder().decode(T.self, from: data)
        } catch {
            throw APIError.decodingError(error)
        }
    }
}
```

### 3.0.5 模块10: 更新ServiceContainer

**需求描述**: 
更新ServiceContainer，正确管理Tesla OAuth和用户Token。

**实现代码**:

```swift
import Foundation
import SwiftData

@Observable
final class ServiceContainer {
    static let shared = ServiceContainer()
    
    let locationService: LocationManager
    let weatherService: WeatherKitService
    let notificationService: NotificationService
    let scheduleService: ScheduleService
    let purchaseManager: PurchaseManager
    let teslaOAuth: TeslaOAuthManager
    
    private var teslaClient: TeslaAPIClient?
    private var vehicleService: VehicleService?
    
    private init() {
        self.locationService = LocationManager()
        self.weatherService = WeatherKitService()
        self.notificationService = NotificationService()
        self.scheduleService = ScheduleService()
        self.purchaseManager = PurchaseManager()
        self.teslaOAuth = TeslaOAuthManager.shared
        self.teslaClient = TeslaAPIClient()
        self.vehicleService = VehicleService(teslaClient: self.teslaClient!)
    }
    
    func getVehicleService() -> VehicleService? {
        return vehicleService
    }
    
    func isTeslaAuthenticated() -> Bool {
        return teslaOAuth.isAuthenticated
    }
    
    func signOutTesla() {
        teslaOAuth.signOut()
    }
}
```

**测试用例**:

```swift
import Testing

@Suite("Tesla OAuth Tests")
struct TeslaOAuthTests {
    @Test("Auth URL is correctly formatted")
    func testAuthURLFormat() {
        let manager = TeslaOAuthManager.shared
        let url = manager.buildAuthorizeURL()
        #expect(url.contains("auth.tesla.com"))
        #expect(url.contains("client_id="))
        #expect(url.contains("response_type=code"))
    }
    
    @Test("Keychain save and retrieve token")
    func testKeychainStorage() async throws {
        let response = TeslaAuthResponse(
            accessToken: "test_access_token",
            refreshToken: "test_refresh_token",
            expiresIn: 3600,
            tokenType: "Bearer"
        )
        try KeychainTokenStorage.saveAuthResponse(response)
        let saved = try KeychainTokenStorage.getAccessToken()
        #expect(saved == "test_access_token")
        try KeychainTokenStorage.clearAll()
    }
}
```

---

## 四、改进方案详细设计

### 4.1 模块1: 位置服务 (LocationManager)

**需求描述**: 
实现完整的位置权限请求流程，为天气服务提供坐标数据。

**功能模块原则**:
- 单一职责: 仅负责位置权限和坐标获取
- 高内聚低耦合: 独立模块，通过协议暴露接口
- 命名清晰: `LocationManager`
- 文件结构: `Core/Services/LocationManager.swift`

**实现代码**:

```swift
import Foundation
import CoreLocation

@Observable
final class LocationManager: NSObject, Observable {
    private let locationManager = CLLocationManager()
    private var continuation: CheckedContinuation<CLLocation, Error>?
    
    var authorizationStatus: CLAuthorizationStatus = .notDetermined
    var currentLocation: CLLocation?
    
    override init() {
        super.init()
        locationManager.delegate = self
        locationManager.desiredAccuracy = kCLLocationAccuracyKilometer
    }
    
    func requestPermission() async throws -> CLLocation {
        guard CLLocationManager.locationServicesEnabled() else {
            throw LocationError.servicesDisabled
        }
        
        return try await withCheckedThrowingContinuation { continuation in
            self.continuation = continuation
            locationManager.requestWhenInUseAuthorization()
        }
    }
    
    func getCurrentLocation() async throws -> CLLocation {
        if let location = currentLocation {
            return location
        }
        return try await requestPermission()
    }
}

// MARK: - CLLocationManagerDelegate
extension LocationManager: CLLocationManagerDelegate {
    func locationManager(_ manager: CLLocationManager, didUpdateLocations locations: [CLLocation]) {
        guard let location = locations.first else { return }
        currentLocation = location
        continuation?.resume(returning: location)
        continuation = nil
    }
    
    func locationManager(_ manager: CLLocationManager, didFailWithError error: Error) {
        continuation?.resume(throwing: error)
        continuation = nil
    }
    
    func locationManagerDidChangeAuthorization(_ manager: CLLocationManager) {
        authorizationStatus = manager.authorizationStatus
        if authorizationStatus == .denied || authorizationStatus == .restricted {
            continuation?.resume(throwing: LocationError.denied)
            continuation = nil
        }
    }
}

enum LocationError: LocalizedError {
    case servicesDisabled
    case denied
    case unavailable
    
    var errorDescription: String? {
        switch self {
        case .servicesDisabled: return "Location services are disabled"
        case .denied: return "Location access denied"
        case .unavailable: return "Location unavailable"
        }
    }
}
```

**测试用例**:

```swift
import Testing
import CoreLocation

@Suite("LocationManager Tests")
struct LocationManagerTests {
    @Test("Initialization sets default state")
    func testInitialization() {
        let manager = LocationManager()
        #expect(manager.authorizationStatus == .notDetermined)
        #expect(manager.currentLocation == nil)
    }
    
    @Test("Location error descriptions are user-friendly")
    func testErrorDescriptions() {
        #expect(LocationError.servicesDisabled.errorDescription != nil)
        #expect(LocationError.denied.errorDescription != nil)
    }
}
```

**注释规范**:
- 公共API必须有文档注释
- 复杂逻辑添加行内注释
- 错误处理添加说明注释

---

### 3.2 模块2: WeatherKit服务 (WeatherKitService)

**需求描述**: 
替换现有OpenWeatherMap实现，使用Apple WeatherKit获取天气数据。

**功能模块原则**:
- 复用现有WeatherData模型（保持数据结构一致）
- 实现WeatherServiceProtocol协议（便于后期扩展其他品牌）
- 文件结构: `Core/Services/WeatherKitService.swift`

**实现代码**:

```swift
import Foundation
import WeatherKit

@Observable
final class WeatherKitService {
    private let weatherService = WeatherService()
    private var cache: [String: (data: WeatherData, timestamp: Date)] = [:]
    private let cacheDuration: TimeInterval = 1800
    
    func getWeatherForLocation(_ location: CLLocation) async throws -> WeatherData {
        let cacheKey = "\(location.coordinate.latitude),\(location.coordinate.longitude)"
        if let cached = cache[cacheKey], Date().timeIntervalSince(cached.timestamp) < cacheDuration {
            return cached.data
        }
        
        let weather = try await weatherService.weather(for: location)
        let weatherData = mapToWeatherData(weather)
        cache[cacheKey] = (data: weatherData, timestamp: Date())
        return weatherData
    }
    
    private func mapToWeatherData(_ weather: Weather) -> WeatherData {
        let current = CurrentWeather(
            temp: weather.currentWeather.temperature.converted(to: .fahrenheit).value,
            feelsLike: weather.currentWeather.apparentTemperature.converted(to: .fahrenheit).value,
            humidity: Int(weather.currentWeather.humidity * 100),
            windSpeed: weather.currentWeather.wind.speed.converted(to: .milesPerHour).value,
            condition: mapCondition(weather.currentWeather.condition),
            description: weather.currentWeather.condition.description
        )
        
        let forecast = weather.dailyForecast.forecast.prefix(7).map { day in
            ForecastItem(
                dateTime: day.date,
                tempMin: day.lowTemperature.converted(to: .fahrenheit).value,
                tempMax: day.highTemperature.converted(to: .fahrenheit).value,
                condition: mapCondition(day.condition),
                pop: day.precipitationChance
            )
        }
        
        return WeatherData(current: current, forecast: Array(forecast))
    }
    
    private func mapCondition(_ condition: WeatherCondition) -> WeatherCondition {
        switch condition {
        case .clear, .mostlyClear: return .clear
        case .partlyCloudy: return .cloudy
        case .mostlyCloudy, .cloudy: return .cloudy
        case .rain, .drizzle: return .lightRain
        case .heavyRain, .thunderstorm: return .heavyRain
        case .snow, .blizzard: return .snow
        case .foggy, .smoky, .hazy: return .fog
        case .windy: return .windy
        default: return .cloudy
        }
    }
}
```

**代码复用说明**:
- 复用现有 `WeatherData` 模型
- 复用现有 `WeatherCondition` 枚举
- 复用现有缓存逻辑模式

---

### 3.3 模块3: 服务容器 (ServiceContainer)

**需求描述**: 
消除硬编码，引入统一的服务管理机制。

**功能模块原则**:
- 单一职责: 仅负责服务初始化和依赖注入
- 使用@Observable支持SwiftUI响应式
- 文件结构: `Core/Services/ServiceContainer.swift`

**实现代码**:

```swift
import Foundation
import SwiftData

@Observable
final class ServiceContainer {
    static let shared = ServiceContainer()
    
    let locationService: LocationManager
    let weatherService: WeatherKitService
    let notificationService: NotificationService
    let scheduleService: ScheduleService
    let purchaseManager: PurchaseManager
    
    private var teslaClient: TeslaAPIClient?
    private var vehicleService: VehicleService?
    
    private init() {
        self.locationService = LocationManager()
        self.weatherService = WeatherKitService()
        self.notificationService = NotificationService()
        self.scheduleService = ScheduleService()
        self.purchaseManager = PurchaseManager()
    }
    
    func configureTesla(clientId: String, clientSecret: String) {
        self.teslaClient = TeslaAPIClient(clientId: clientId, clientSecret: clientSecret)
        self.vehicleService = VehicleService(teslaClient: self.teslaClient!)
    }
    
    func getVehicleService() -> VehicleService? {
        return vehicleService
    }
}
```

**使用示例 (替换DashboardViewModel硬编码)**:

```swift
@Observable
final class DashboardViewModel {
    private let services = ServiceContainer.shared
    
    func quickCool(vehicle: Vehicle) {
        Task {
            guard let vehicleService = services.getVehicleService() else { return }
            try? await vehicleService.startPreconditioning(vehicle: vehicle)
        }
    }
    
    // ... 其他方法类似
}
```

---

### 3.4 模块4: Info.plist配置更新

**需求描述**: 
补充所有缺失的权限描述和后台任务配置。

**配置清单**:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- 现有配置 -->
    <key>UIBackgroundModes</key>
    <array>
        <string>fetch</string>
        <string>processing</string>
    </array>
    
    <!-- 新增: 位置权限描述 -->
    <key>NSLocationWhenInUseUsageDescription</key>
    <string>We need your location to provide accurate weather data for smart preconditioning.</string>
    
    <!-- 新增: 后台任务标识 -->
    <key>BGTaskSchedulerPermittedIdentifiers</key>
    <array>
        <string>com.zzoutuo.PrecondAI.precondition-task</string>
    </array>
</dict>
</plist>
```

**验证标准**:
- [ ] 构建成功无警告
- [ ] 运行时权限弹窗正常显示
- [ ] 后台任务注册成功（控制台无警告）

---

### 3.5 模块5: Xcode Capabilities配置

**需求描述**: 
在Xcode项目中添加WeatherKit Capability。

**配置步骤**:
1. 打开 `PrecondAI.xcodeproj`
2. 选择 `PrecondAI` Target → **Signing & Capabilities**
3. 点击 **+ Capability** → 添加 **WeatherKit**
4. 验证 `PrecondAI.entitlements` 包含:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>aps-environment</key>
    <string>development</string>
    <key>com.apple.developer.weatherkit</key>
    <true/>
</dict>
</plist>
```

---

## 五、代码清理计划

### 5.1 废弃代码标记

| 文件 | 操作 | 原因 |
|------|------|------|
| `WeatherAPIClient.swift` | 标记@available(iOS, deprecated) | 替换为WeatherKitService |
| `WeatherService.swift` (旧) | 删除 | 功能已合并到WeatherKitService |
| `TeslaAPIClient.swift` (旧init) | 修改 | 移除clientId/clientSecret参数，改用Keychain |
| `DashboardViewModel` (硬编码) | 删除 | 改用ServiceContainer |

### 5.2 清理流程

```
1. 标记废弃 → 添加@available(deprecated, message: "Use WeatherKitService instead")
2. 验证无影响 → 确保所有引用已更新
3. 删除文件 → 从项目中移除
4. 提交信息 → "refactor: replace OpenWeatherMap with WeatherKit, remove deprecated WeatherAPIClient"
```

### 5.3 提交信息规范

```
feat: add Tesla OAuth, WeatherKit, and location services

- Add TeslaOAuthManager with OAuth 2.0 PKCE flow
- Add KeychainTokenStorage for secure token management
- Add LocationManager with permission handling
- Replace OpenWeatherMap with Apple WeatherKit
- Add ServiceContainer for dependency injection
- Update Info.plist with required permissions
- Refactor VehicleAuthView with Tesla OAuth button

Cleanup scope:
- Deleted: Core/Network/WeatherAPIClient.swift
- Deleted: Core/Services/WeatherService.swift (old)
- Modified: TeslaAPIClient (removed hardcoded credentials)
- Modified: DashboardViewModel (removed hardcoding)
- Modified: VehicleAuthView (added OAuth flow)
```

---

## 六、继承与兼容性检查

### 6.1 SettingsView继承关系

**当前实现**: SettingsView已包含订阅、偏好设置、支持链接

**优化继承**:
- 新增"天气单位"设置（已存在temperatureUnit）
- 新增"位置权限"管理入口
- 新增"Tesla账户"管理入口（断开连接/重新授权）
- 保持现有UI风格一致

**兼容性检查**:
- [ ] 现有@AppStorage键值不冲突
- [ ] 现有NavigationLink路径不变
- [ ] Paywall集成不受影响

### 6.2 DashboardView兼容性

**当前实现**: 使用DashboardViewModel调用TeslaAPIClient

**优化后**:
- 通过ServiceContainer获取VehicleService
- 保持UI层代码不变
- ViewModel内部实现替换

### 6.3 VehicleAuthView兼容性

**当前实现**: 仅保存本地Vehicle信息，无真实授权

**优化后**:
- 保留现有表单字段（brand/model/year/displayName）
- 新增Tesla OAuth授权按钮
- 授权成功后自动填充VIN和车辆信息
- 向后兼容：非Tesla品牌仍使用原有流程

### 6.4 Vehicle模型扩展

**需要新增字段**:
```swift
@Model
final class Vehicle {
    // 现有字段...
    var vin: String?              // 新增：车辆VIN码
    var isTeslaConnected: Bool    // 新增：Tesla OAuth状态
    var lastAuthDate: Date?       // 新增：最后授权时间
}
```

---

## 七、测试计划

### 7.1 单元测试

| 测试模块 | 测试用例 | 预期结果 |
|---------|---------|---------|
| TeslaOAuthManager | Auth URL格式 | 包含auth.tesla.com和code参数 |
| TeslaOAuthManager | PKCE code challenge | 生成有效的S256 challenge |
| KeychainTokenStorage | Token保存和读取 | 成功存储并检索 |
| KeychainTokenStorage | Token清除 | 成功删除所有token |
| LocationManager | 权限请求 | 返回授权状态 |
| LocationManager | 位置获取 | 返回有效坐标 |
| WeatherKitService | 天气数据映射 | 正确转换为WeatherData |
| ServiceContainer | 服务初始化 | 所有服务可用 |

### 7.2 UI测试

| 测试场景 | 操作步骤 | 预期结果 |
|---------|---------|---------|
| Tesla OAuth授权 | 点击"Connect via Tesla" | 弹出Tesla登录页 |
| Tesla OAuth授权 | 完成授权 | 显示已连接状态 |
| 首次启动 | 完成Onboarding | 请求位置权限 |
| 天气显示 | 查看Dashboard | 显示当地天气 |
| 后台任务 | 设置Schedule | 后台正常执行 |

### 7.3 模拟器测试

- [ ] iPhone 16 Pro Max (iOS 18.4)
- [ ] iPad Pro 13-inch (M4)
- [ ] 位置模拟测试
- [ ] 权限拒绝场景测试
- [ ] Tesla OAuth流程测试（需真机或Safari）

---

## 八、实施顺序与里程碑

### Phase 1: Tesla OAuth基础设施 (优先级🔴)
1. 实现KeychainTokenStorage
2. 实现TeslaOAuthManager
3. 更新TeslaAPIClient移除硬编码
4. 更新VehicleAuthView添加OAuth按钮

### Phase 2: 位置与天气服务 (优先级🔴)
1. 更新Info.plist配置
2. 添加WeatherKit Capability
3. 实现LocationManager
4. 实现WeatherKitService

### Phase 3: 架构优化 (优先级🟡)
1. 实现ServiceContainer
2. 更新DashboardViewModel
3. 扩展Vehicle模型字段
4. 更新SettingsView添加Tesla管理

### Phase 4: 清理验证 (优先级🟢)
1. 标记废弃代码
2. 删除旧WeatherAPIClient
3. 运行测试验证
4. 提交代码并注明清理范围

---

## 九、风险与应对

| 风险 | 影响 | 应对措施 |
|------|------|---------|
| Tesla OAuth配置失败 | 无法连接用户车辆 | 保留手动输入VIN作为fallback |
| WeatherKit配置失败 | 天气功能不可用 | 保留OpenWeatherMap作为fallback |
| 位置权限被拒 | 无法获取天气 | 允许手动输入邮编/城市 |
| iOS版本兼容 | WeatherKit需iOS 16+ | 项目已设17.0，无风险 |
| Keychain访问失败 | Token无法存储 | 捕获错误并提示用户重新授权 |

---

## 十、验收标准

### 10.1 功能验收
- [ ] Tesla OAuth授权流程完整
- [ ] Token安全存储在Keychain
- [ ] 位置权限请求正常
- [ ] 天气数据正确显示
- [ ] 后台任务正常执行
- [ ] 订阅功能不受影响

### 10.2 代码质量
- [ ] 无硬编码服务初始化
- [ ] 所有公共API有文档注释
- [ ] 单元测试覆盖率>70%
- [ ] 无废弃代码残留
- [ ] OAuth流程符合PKCE标准

### 10.3 用户体验
- [ ] Tesla授权流程清晰易懂
- [ ] 权限请求时机合理
- [ ] 错误提示友好
- [ ] UI风格一致
- [ ] iPad布局正常

---

## 十一、开发者配置指南

### 11.1 Tesla Developer Portal注册

**步骤**:
1. 访问 [developer.tesla.com](https://developer.tesla.com)
2. 登录Tesla账号
3. 创建新应用，获取`client_id`和`client_secret`
4. 配置Redirect URI: `precondai://auth/tesla/callback`

### 11.2 项目配置

**Info.plist添加**:
```xml
<key>TESLA_CLIENT_ID</key>
<string>你的Tesla Client ID</string>
```

**或使用.xcconfig** (推荐):
```
TESLA_CLIENT_ID = your_client_id_here
TESLA_CLIENT_SECRET = your_client_secret_here
```

**⚠️ 安全提醒**: 
- `client_secret`仅用于服务端，**不要**包含在iOS应用中
- iOS应用只需要`client_id`
- Redirect URI必须与Tesla Developer Portal配置一致

---

## 十二、后续规划

### Phase 2 候选优化
1. 添加多品牌车辆支持（Ford, BMW）
2. 实现Apple Watch companion app
3. 添加Siri快捷指令
4. 实现iCloud数据同步
5. 支持Tesla Fleet API多车辆管理

---

**文档版本**: 1.0  
**最后更新**: 2026-04-27  
**负责人**: AI Assistant  
**状态**: 待审核
